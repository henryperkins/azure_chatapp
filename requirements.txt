# Web framework & ASGI server
fastapi>=0.110
uvicorn[standard]>=0.27
slowapi>=0.1.9

# Data-validation / models
pydantic>=1.10,<2.0   # (code still uses V1 style)

# HTTP client, form-data handling
httpx>=0.26
python-multipart>=0.0.6
aiofiles>=23.0.0

# ORM & database drivers
sqlalchemy>=2.0
greenlet>=2.0
asyncpg>=0.29        # async Postgres driver used by SQLAlchemy
psycopg2-binary>=2.9.0  # PostgreSQL adapter for Python

# Auth / JWT
PyJWT>=2.8

# Monitoring / tracing
sentry-sdk>=1.39
psutil>=5.9          # optional but referenced for memory metrics

# Markdown / sanitising
markdown2>=2.4
bleach>=6.0

# CLI / coloured output
colorama>=0.4

# Token counting (optional, auto-disabled if absent but recommended)
tiktoken>=0.6

# Vector database and ML dependencies (optional but recommended for local embeddings)
numpy>=1.24.0
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
scikit-learn>=1.3.0

# File processing and text extraction
python-docx>=0.8.11
PyPDF2>=3.0.0
openpyxl>=3.1.0
python-pptx>=0.6.21

# Additional utilities
python-dotenv>=1.0.0
Pillow>=10.0.0

# Development and testing dependencies (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
bandit>=1.7.5
safety>=2.3.0
